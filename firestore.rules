
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // WARNING: Insecure rule. Allows public access.
    // This is for debugging purposes only.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
